doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title Books
    link(rel='stylesheet', href='/css/post.css')
  body
      .header
        .header_brand
            h1.header_brand Books
        .header_users
          .header_user
            h1.header_user #{nickname}
          a.header_logout-button(href='/auth/logout') Log Out
      .body
        label(for="title") 책이름
        input.title(type="text", name="title", value=book.title)
        label(for="title") 지은이
        input.author(type="text", name="author", value=book.author)
        label(for="publisher") 출판사
        input.publisher(type="text", name="publisher", value=book.publisher)
        label(for="publicationDate") 출판일
        input.publicationDate(type="date", name="publicationDate", value=publicationDate)
        textarea(name="description", cols="30", rows="10").description #{book.description}
        button.submit-button(id=book._id) 완료
        script(src="/javascript/update.js")